# Server
PORT=3000

# OCR threshold (0-100). Jika confidence OCR di bawah ini, pakai AI fallback
OCR_CONFIDENCE_THRESHOLD=65

# OpenAI (opsional — hanya dipakai jika OCR confidence rendah)
OPENAI_API_KEY=sk-...

# OpenAI model untuk fallback (hemat token vs gpt-4-vision)
OPENAI_MODEL=gpt-4o-mini
